import pandas as pd

def level_score(raw_data: pd.DataFrame) -> list:
    """Compute 'entry levelness' scores for each job post in raw_data"""

    ## TODO: implementation

    return [0] * len(raw_data)
